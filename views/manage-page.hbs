<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel='stylesheet' href='/stylesheets/allevents.css' />
  <title>Document</title>
</head>
<body>


<div class="main-contents-events">
  <div class="title-div">

    <h1 class="events-title">Manage Your Events</h1>
  </div>

{{!-- Sébastien's part --}}

{{!-- <h1>Here are your events</h1>
<div style="display:flex;flex-wrap:wrap">
  {{#each events}}
  <div style="width:20%">
    <img style="width:20%;height:auto" src="{{this.imgPath}}" alt="">
    <h4>{{this.eventName}}</h4>
    <p>{{this.dateOfEvent}}</p>
  </div>
  {{else}}
  <p>You have no planned event at the moment</p>
  {{/each}}
</div> --}}

    <section class="events-wrapper" style="flex-wrap: wrap;">

    {{#each allEvents}}
    <div class="events-info" style="width:18%; margin:1%">
        <p>As a Host</p>

      <img class="img_events_all" src="{{this.imgPath}}" alt="events_img">

      <h3>{{this.eventName}}</h3>
      <h4 class="events-title">{{this.type}}</h4>
      <p>Date{{this.date}}</p>
      <p>Diet{{this.restrictions}}</p>
      <button>Cancel</button> <button>Edit</button>
   
    </div>
    {{/each}}

  </section>

  <section class="events-wrapper">

    {{#each allEvents}}
    <div class="events-info">
        <p>As a Guest</p>

      <img class="img_events_all" src="{{this.imgPath}}" alt="events_img">

      <h3>{{this.eventName}}</h3>
      <h4 class="events-title">{{this.type}}</h4>
      <p>Date{{this.date}}</p>
      <p>Diet{{this.restrictions}}</p>
     <button>Cancel</button>
    </div>
    {{/each}}

  </section>
</div>

  
</body>
</html>